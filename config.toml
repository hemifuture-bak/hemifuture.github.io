title = "Hemifuture"
subtitle = "Magic Design for Good"
keywords = ""
copyright = ""


baseurl = "/"
languageCode = "en-us"
theme = "terminal"
paginate = 5

[menu]
  [[menu.main]]
    identifier = "about"
    name = "About"
    url = "/about"
  [[menu.main]]
    identifier = "github"
    name = "Github"
    url = "https://github.com/dyzdyz010"

[params]
  readMore = "Read more"
  menuMore = "Show more"
  readOtherPosts = "Read other posts"
  # dir name of your blog content (default is `content/posts`)
  contentTypeName = "post"
  # ["orange", "blue", "red", "green", "pink"]
  themeColor = "orange"
  # if you set this to 0, only submenu trigger will be visible
  showMenuItems = 4
  # set theme to full screen width
  fullWidthTheme = false
  # center theme with default width
  centerTheme = true
  # set a custom favicon (default is a `themeColor` square)
  # favicon = "favicon.ico"
    [params.logo]
      logoText = "Hemifuture"
      logoHomeLink = "/"